Проверь, доступен ли диск X по пути X:\Research\VS (например, через Test-Path или попытку получить список файлов).

Если диск доступен — выведи ровно одну строку: "OK: диск X доступен, время проверки <текущее время>".

Если диск НЕ доступен — выведи ровно одну строку, начинающуюся со знака ⚠️: "⚠️ диск X недоступен на момент проверки <текущее время>, нужна ручная синхронизация позже".

Больше ничего не делай: не читай и не пиши другие файлы, не используй веб-поиск, только эту одну проверку и один ответ.
